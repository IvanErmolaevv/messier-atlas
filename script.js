const DATA_ROOT='https://raw.githubusercontent.com/ofrohn/d3-celestial/master/data/';
const urls={stars:`${DATA_ROOT}stars.6.json`,lines:`${DATA_ROOT}constellations.lines.json`,messier:`${DATA_ROOT}messier.json`,names:`${DATA_ROOT}starnames.json`};
const constellationNames={And:'Андромеда',Ant:'Насос',Aps:'Райская Птица',Aqr:'Водолей',Aql:'Орёл',Ara:'Жертвенник',Ari:'Овен',Aur:'Возничий',Boo:'Волопас',Cae:'Резец',Cam:'Жираф',Cnc:'Рак',CVn:'Гончие Псы',CMa:'Большой Пёс',CMi:'Малый Пёс',Cap:'Козерог',Car:'Киль',Cas:'Кассиопея',Cen:'Центавр',Cep:'Цефей',Cet:'Кит',Cha:'Хамелеон',Cir:'Циркуль',Col:'Голубь',Com:'Волосы Вероники',CrA:'Южная Корона',CrB:'Северная Корона',Crv:'Ворон',Crt:'Чаша',Cru:'Южный Крест',Cyg:'Лебедь',Del:'Дельфин',Dor:'Золотая Рыба',Dra:'Дракон',Equ:'Малый Конь',Eri:'Эридан',For:'Печь',Gem:'Близнецы',Gru:'Журавль',Her:'Геркулес',Hor:'Часы',Hya:'Гидра',Hyi:'Южная Гидра',Ind:'Индеец',Lac:'Ящерица',Leo:'Лев',LMi:'Малый Лев',Lep:'Заяц',Lib:'Весы',Lup:'Волк',Lyn:'Рысь',Lyr:'Лира',Men:'Столовая Гора',Mic:'Микроскоп',Mon:'Единорог',Mus:'Муха',Nor:'Наугольник',Oct:'Октант',Oph:'Змееносец',Ori:'Орион',Pav:'Павлин',Peg:'Пегас',Per:'Персей',Phe:'Феникс',Pic:'Живописец',Psc:'Рыбы',PsA:'Южная Рыба',Pup:'Корма',Pyx:'Компас',Ret:'Сетка',Sge:'Стрела',Sgr:'Стрелец',Sco:'Скорпион',Scl:'Скульптор',Sct:'Щит',Ser:'Змея',Sex:'Секстант',Tau:'Телец',Tel:'Телескоп',Tri:'Треугольник',TrA:'Южный Треугольник',Tuc:'Тукан',UMa:'Большая Медведица',UMi:'Малая Медведица',Vel:'Паруса',Vir:'Дева',Vol:'Летучая Рыба',Vul:'Лисичка'};
const typeNames={gc:'шаровое скопление',oc:'рассеянное скопление',g:'галактика',sfr:'туманность',pn:'планетарная туманность',snr:'остаток сверхновой',dn:'тёмная туманность',en:'эмиссионная туманность',rn:'отражательная туманность'};
const familiarMessier={M1:'Крабовидная туманность',M6:'Бабочка',M7:'Скопление Птолемея',M8:'Лагуна',M13:'Большое скопление Геркулеса',M20:'Трёхраздельная туманность',M27:'Гантель',M31:'Галактика Андромеды',M33:'Галактика Треугольника',M42:'Туманность Ориона',M45:'Плеяды',M51:'Водоворот',M57:'Кольцевая туманность',M64:'Чёрный Глаз',M81:'Галактика Боде',M82:'Сигара',M101:'Вертушка',M104:'Сомбреро'};
const lessons=[
  {id:'sagittarius',title:'Стрелец: туманности Млечного Пути',description:'Четыре объекта у «чайника» Стрельца. Найдите их по реальным звёздам и правильной фигуре созвездия.',ids:['M8','M20','M22','M24'],fov:36},
  {id:'hercules',title:'Геркулес и Лира',description:'Три классических объекта между Геркулесом и Лирой — хорошая карта для первого маршрута.',ids:['M13','M92','M57'],fov:36},
  {id:'arrow',title:'Стрела и Лисичка',description:'Компактная летняя область вокруг туманности Гантель и двух скоплений.',ids:['M27','M56','M71'],fov:36},
  {id:'auriga',title:'Возничий и Близнецы',description:'Серия рассеянных скоплений в одном небольшом зимнем поле.',ids:['M35','M36','M37','M38'],fov:36},
  {id:'spring',title:'Весенние галактики',description:'Галактики в районе Большой Медведицы и Гончих Псов помещаются в одно поле 40°.',ids:['M51','M63','M94','M101','M106'],fov:40},
  {id:'virgo',title:'Скопление галактик в Деве',description:'Несколько галактик каталога Мессье в компактном поле Девы.',ids:['M58','M84','M86','M87','M89','M90'],fov:36},
  {id:'andromeda',title:'Андромеда и Треугольник',description:'Галактика Андромеды с её спутниками и галактика Треугольника.',ids:['M31','M32','M33','M110'],fov:36},
  {id:'scorpius',title:'Скорпион: яркие скопления',description:'Четыре объекта рядом с изгибом Скорпиона — все в одной реальной области неба.',ids:['M4','M6','M7','M80'],fov:38}
];

const homeCanvas=document.querySelector('#home-canvas');
const atlasCanvas=document.querySelector('#atlas-canvas');
const mapStage=document.querySelector('.map-stage');
const missionPanel=document.querySelector('#mission-panel');
const mapLoading=document.querySelector('#map-loading');
const toast=document.querySelector('#toast');
const controls={figures:document.querySelector('#figures-toggle'),names:document.querySelector('#names-toggle'),grid:document.querySelector('#grid-toggle'),objects:document.querySelector('#objects-toggle')};
const sky={ready:false,stars:[],lines:[],messier:[],names:{}};
let mode='learn',field={center:[-109.3,36.5],focusId:'M13',fov:36,lesson:null},currentTarget=null,learnPhase='study',placedMarkers={},learnResults={},practiceResult=null,score=0,streak=0,toastTimer,dragState=null,suppressClick=false;

const radians=(d)=>d*Math.PI/180;
const degrees=(r)=>r*180/Math.PI;
const clamp=(n,min,max)=>Math.min(max,Math.max(min,n));
const wrapLon=(lon)=>((lon+180)%360+360)%360-180;
const randomBetween=(min,max)=>Math.random()*(max-min)+min;
const objectById=(id)=>sky.messier.find((object)=>object.id===id);
const currentObject=()=>objectById(field.focusId);
const objectName=(object)=>familiarMessier[object.id]||object.properties.alt||object.properties.desig||'объект каталога';
const objectType=(object)=>typeNames[object.properties.type]||'объект глубокого неба';

function setView(name){['home','tour','atlas'].forEach((view)=>{const element=document.querySelector(`#${view}-view`);element.hidden=view!==name;element.classList.toggle('is-active',view===name);});window.scrollTo({top:0,behavior:'auto'});requestAnimationFrame(renderAll);}
function project(coordinates,center,radius,cx,cy,fov=180){const [lon,lat]=coordinates,[lon0,lat0]=center,lambda=radians(lon),phi=radians(lat),lambda0=radians(lon0),phi0=radians(lat0),delta=lambda-lambda0,cosc=Math.sin(phi0)*Math.sin(phi)+Math.cos(phi0)*Math.cos(phi)*Math.cos(delta),half=radians(fov/2);if(cosc<Math.cos(half))return null;const scale=radius/Math.sin(half);return{x:cx-scale*Math.cos(phi)*Math.sin(delta),y:cy-scale*(Math.cos(phi0)*Math.sin(phi)-Math.sin(phi0)*Math.cos(phi)*Math.cos(delta)),cosc};}
function inverseProject(x,y,center,radius,fov=180){const half=radians(fov/2),rho=Math.hypot(x,y)/radius*Math.sin(half);if(rho>Math.sin(half))return null;const [lon0,lat0]=center,phi0=radians(lat0),lambda0=radians(lon0),c=Math.asin(rho);if(rho===0)return center;const north=-y/radius*Math.sin(half),phi=Math.asin(Math.cos(c)*Math.sin(phi0)+north*Math.sin(c)*Math.cos(phi0)/rho),lambda=lambda0+Math.atan2(-(x/radius)*Math.sin(half)*Math.sin(c),rho*Math.cos(phi0)*Math.cos(c)-north*Math.sin(phi0)*Math.sin(c));return[wrapLon(degrees(lambda)),degrees(phi)];}
function sphericalDistance(a,b){const[a1,a2]=a.map(radians),[b1,b2]=b.map(radians);return degrees(Math.acos(clamp(Math.sin(a2)*Math.sin(b2)+Math.cos(a2)*Math.cos(b2)*Math.cos(a1-b1),-1,1)));}
function circlePath(ctx,points,center,radius,fov){let drawing=false;ctx.beginPath();points.forEach((coord)=>{const point=project(coord,center,radius,0,0,fov);if(!point){drawing=false;return;}if(!drawing)ctx.moveTo(point.x,point.y);else ctx.lineTo(point.x,point.y);drawing=true;});ctx.stroke();}
function starColor(bv){if(bv===undefined||bv===null)return'#6e83a3';if(bv<.1)return'#4777c5';if(bv<.6)return'#536b8d';if(bv<1.15)return'#8d7b5a';return'#9a6932';}
function starRadius(mag,fov){return clamp((4.4-(mag+1.2)*.57)*(fov<60?1.18:1),.52,5.4);}
function resizeCanvas(canvas){const rect=canvas.getBoundingClientRect();if(!rect.width||!rect.height)return null;const dpr=Math.min(window.devicePixelRatio||1,2),width=Math.round(rect.width*dpr),height=Math.round(rect.height*dpr);if(canvas.width!==width||canvas.height!==height){canvas.width=width;canvas.height=height;}const ctx=canvas.getContext('2d');ctx.setTransform(dpr,0,0,dpr,0,0);return{ctx,width:rect.width,height:rect.height};}

function drawGrid(ctx,center,radius,cx,cy,fov){ctx.save();ctx.translate(cx,cy);ctx.strokeStyle='rgba(127,155,210,.22)';ctx.lineWidth=.65;const step=fov<=50?5:30;for(let dec=-85;dec<=85;dec+=step){const coords=[];for(let lon=-180;lon<=180;lon+=1.5)coords.push([lon,dec]);circlePath(ctx,coords,center,radius,fov);}for(let lon=-180;lon<180;lon+=step){const coords=[];for(let dec=-89;dec<=89;dec+=1.5)coords.push([lon,dec]);circlePath(ctx,coords,center,radius,fov);}ctx.restore();}
function drawConstellations(ctx,center,radius,cx,cy,showLabels,fov){ctx.save();ctx.translate(cx,cy);ctx.strokeStyle='rgba(38,84,150,.72)';ctx.lineWidth=fov<=50?1.25:1.05;ctx.lineCap='round';const labelCandidates=[];sky.lines.forEach((feature)=>{const groups=feature.geometry.coordinates;groups.forEach((line)=>circlePath(ctx,line,center,radius,fov));const projected=groups.flat().map((coord)=>project(coord,center,radius,0,0,fov)).filter(Boolean);if(showLabels&&feature.properties.rank==='1'&&projected.length>3){const x=projected.reduce((sum,p)=>sum+p.x,0)/projected.length,y=projected.reduce((sum,p)=>sum+p.y,0)/projected.length;if(Math.hypot(x,y)<radius*.84)labelCandidates.push({x,y,name:constellationNames[feature.id]||feature.id});}});if(showLabels){ctx.font=`700 ${fov<=50?11:9}px Inter, sans-serif`;ctx.fillStyle='rgba(37,76,135,.64)';ctx.textAlign='center';ctx.textBaseline='middle';const used=[],space=fov<=50?76:54;labelCandidates.forEach((label)=>{if(used.some((point)=>Math.hypot(point.x-label.x,point.y-label.y)<space))return;used.push(label);ctx.fillText(label.name,label.x,label.y);});}ctx.restore();}
function drawStars(ctx,center,radius,cx,cy,showNames,fov){ctx.save();ctx.translate(cx,cy);const named=[];sky.stars.forEach((star)=>{const point=project(star.geometry.coordinates,center,radius,0,0,fov);if(!point)return;const mag=star.properties.mag,r=starRadius(mag,fov);ctx.beginPath();ctx.globalAlpha=clamp(.42+(6-mag)*.16,.4,1);ctx.fillStyle=starColor(star.properties.bv);ctx.arc(point.x,point.y,r,0,Math.PI*2);ctx.fill();if(showNames&&mag<1.75){const name=sky.names[star.id]?.ru||sky.names[star.id]?.name;if(name)named.push({x:point.x,y:point.y,name,mag});}});if(showNames){ctx.globalAlpha=1;ctx.font=`700 ${fov<=50?11:10}px Inter, sans-serif`;ctx.textBaseline='middle';const used=[],space=fov<=50?62:48;named.sort((a,b)=>a.mag-b.mag).forEach((star)=>{const label={x:star.x+9,y:star.y-8};if(used.some((p)=>Math.hypot(p.x-label.x,p.y-label.y)<space))return;used.push(label);ctx.fillStyle='rgba(26,47,91,.9)';ctx.fillText(star.name,label.x,label.y);});}ctx.restore();}
function visibleObjects(center=field.center,fov=field.fov){return sky.messier.map((object)=>({object,point:project(object.geometry.coordinates,center,1,0,0,fov)})).filter(({point})=>point);}
function displayedObjects(center=field.center,fov=field.fov){const objects=visibleObjects(center,fov);return mode==='learn'&&field.lesson?objects.filter(({object})=>field.lesson.ids.includes(object.id)):objects;}
function drawMessier(ctx,center,radius,cx,cy,showLabels,fov){const objects=displayedObjects(center,fov);ctx.save();ctx.translate(cx,cy);ctx.font=`800 ${fov<=50?11:9}px Inter, sans-serif`;ctx.textBaseline='middle';objects.forEach(({object,point})=>{const x=point.x*radius,y=point.y*radius;ctx.beginPath();ctx.fillStyle='#ed625f';ctx.globalAlpha=.16;ctx.arc(x,y,fov<=50?10:8,0,Math.PI*2);ctx.fill();ctx.beginPath();ctx.globalAlpha=1;ctx.fillStyle='#e65755';ctx.arc(x,y,fov<=50?4:3.1,0,Math.PI*2);ctx.fill();if(showLabels){ctx.fillStyle='#b4474e';ctx.fillText(object.id,x+8,y-7);}});ctx.restore();}
function placementIds(){if(mode==='practice')return currentTarget?[currentTarget.id]:[];if(mode==='learn'&&learnPhase!=='study')return field.lesson?.ids||[];return[];}
function drawPlacedMarkers(ctx,center,radius,cx,cy,fov){const ids=placementIds();if(!ids.length)return;ctx.save();ctx.translate(cx,cy);ctx.font=`800 ${fov<=50?11:10}px Inter, sans-serif`;ctx.textBaseline='middle';ids.forEach((id)=>{const coordinates=placedMarkers[id];if(!coordinates)return;const point=project(coordinates,center,radius,0,0,fov);if(!point)return;const result=mode==='learn'?learnResults[id]:practiceResult?.correct?{correct:true}:null,color=result?result.correct?'#35a97a':'#e85f5c':'#1d68e8';ctx.beginPath();ctx.fillStyle='#fff';ctx.strokeStyle=color;ctx.lineWidth=2.5;ctx.arc(point.x,point.y,8,0,Math.PI*2);ctx.fill();ctx.stroke();ctx.beginPath();ctx.fillStyle=color;ctx.arc(point.x,point.y,2.5,0,Math.PI*2);ctx.fill();ctx.fillStyle=color;ctx.fillText(id,point.x+11,point.y-9);});if(mode==='practice'&&practiceResult&&currentTarget){const point=project(currentTarget.geometry.coordinates,center,radius,0,0,fov);if(point){ctx.strokeStyle=practiceResult.correct?'#35a97a':'#e85f5c';ctx.lineWidth=2.5;ctx.setLineDash([4,3]);ctx.beginPath();ctx.arc(point.x,point.y,16,0,Math.PI*2);ctx.stroke();ctx.setLineDash([]);}}ctx.restore();}
function drawSky(canvas,center,options={}){const sized=resizeCanvas(canvas);if(!sized)return;const{ctx,width,height}=sized,fov=options.fov??180,radius=Math.min(width,height)*.462,cx=width/2,cy=height/2;ctx.clearRect(0,0,width,height);ctx.save();ctx.beginPath();ctx.arc(cx,cy,radius,0,Math.PI*2);ctx.clip();ctx.fillStyle='#fcfdff';ctx.fillRect(0,0,width,height);ctx.translate(cx,cy);if(options.grid)drawGrid(ctx,center,radius,0,0,fov);drawStars(ctx,center,radius,0,0,options.names,fov);if(options.figures)drawConstellations(ctx,center,radius,0,0,options.names,fov);if(options.objects)drawMessier(ctx,center,radius,0,0,options.objectLabels,fov);drawPlacedMarkers(ctx,center,radius,0,0,fov);ctx.restore();ctx.beginPath();ctx.arc(cx,cy,radius,0,Math.PI*2);ctx.strokeStyle='#233b6f';ctx.lineWidth=1.35;ctx.stroke();return{radius,cx,cy,width,height,fov};}

function formatCoordinates([lon,lat]){const hours=((lon+360)%360)/15,h=Math.floor(hours),min=Math.round((hours-h)*60);return`ЦЕНТР: RA ${String(h).padStart(2,'0')}h ${String(min).padStart(2,'0')}m · DEC ${lat>=0?'+':''}${lat.toFixed(0)}°`;}
function averageCenter(objects){let x=0,y=0,z=0;objects.forEach((object)=>{const[lon,lat]=object.geometry.coordinates,lambda=radians(lon),phi=radians(lat);x+=Math.cos(phi)*Math.cos(lambda);y+=Math.cos(phi)*Math.sin(lambda);z+=Math.sin(phi);});const total=objects.length,lambda=Math.atan2(y/total,x/total),phi=Math.atan2(z/total,Math.hypot(x/total,y/total));return[wrapLon(degrees(lambda)),degrees(phi)];}
function chooseLesson(){if(!sky.ready)return;let lesson=lessons[Math.floor(Math.random()*lessons.length)];if(field.lesson&&lessons.length>1){while(lesson.id===field.lesson.id)lesson=lessons[Math.floor(Math.random()*lessons.length)];}const objects=lesson.ids.map(objectById).filter(Boolean);field={center:averageCenter(objects),focusId:objects[0].id,fov:lesson.fov,lesson};learnPhase='study';placedMarkers={};learnResults={};practiceResult=null;}
function choosePracticeField(){if(!sky.ready)return;let target=sky.messier[Math.floor(Math.random()*sky.messier.length)];if(currentTarget&&sky.messier.length>1){while(target.id===currentTarget.id)target=sky.messier[Math.floor(Math.random()*sky.messier.length)];}const[lon,lat]=target.geometry.coordinates;field={center:[wrapLon(lon+randomBetween(-24,24)),clamp(lat+randomBetween(-16,16),-72,72)],focusId:target.id,fov:180,lesson:null};currentTarget=target;placedMarkers={};practiceResult=null;}
function chooseCatalogField(){if(!sky.ready)return;field={center:[-97,31],focusId:'M13',fov:180,lesson:null};placedMarkers={};practiceResult=null;}
function showToast(message){toast.textContent=message;toast.classList.add('is-visible');clearTimeout(toastTimer);toastTimer=setTimeout(()=>toast.classList.remove('is-visible'),3300);}
function tokenStyle(id){const result=mode==='learn'?learnResults[id]:practiceResult?.correct?{correct:true}:null,color=result?result.correct?'#35a97a':'#e85f5c':'#1d68e8',status=placedMarkers[id]?result?(result.correct?'✓ верно':'передвиньте'):'на карте':'перетащите на карту';return `<button class="panel-subbutton" data-marker-token data-marker-id="${id}" type="button" style="margin-top:7px;border-color:${color};color:${color};display:flex;justify-content:space-between;align-items:center"><b>${id}</b><span>${status}</span></button>`;}
function markerTray(ids,label='МЕТКИ ДЛЯ ПЕРЕНОСА'){return `<div class="field-card"><span class="field-card__label">${label}</span><small style="margin-top:7px">Перетащите каждую метку на нужное место карты. Метки закрепляются за небесными координатами и вращаются вместе с небом.</small>${ids.map(tokenStyle).join('')}</div>`;}
function startLessonCheck(){learnPhase='quiz';placedMarkers={};learnResults={};renderAtlas();showToast('Перетащите все метки из боковой панели на карту.');}
function setPlacedMarker(id,coordinates){placedMarkers={...placedMarkers,[id]:coordinates};if(mode==='learn')learnResults={};if(mode==='practice')practiceResult=null;}
function checkLesson(){const ids=field.lesson?.ids||[],missing=ids.filter((id)=>!placedMarkers[id]);if(missing.length){showToast(`Сначала расставьте все метки: осталось ${missing.join(', ')}.`);return;}const results={};ids.forEach((id)=>{results[id]={distance:sphericalDistance(placedMarkers[id],objectById(id).geometry.coordinates)};results[id].correct=results[id].distance<=2.5;});learnResults=results;const correct=ids.every((id)=>results[id].correct);learnPhase=correct?'passed':'quiz';renderAtlas();showToast(correct?'Участок пройден — все объекты расставлены верно!':'Пока не все метки точны. Передвиньте красные метки и проверьте снова.');}
function checkPractice(){const id=currentTarget?.id;if(!id||!placedMarkers[id]){showToast('Сначала перетащите метку цели на карту.');return;}const distance=sphericalDistance(placedMarkers[id],currentTarget.geometry.coordinates);practiceResult={correct:distance<=2.5,distance};if(practiceResult.correct){score++;streak++;showToast(`Верно: ${id} найден с точностью ${distance.toFixed(1)}°.`);}else{streak=0;showToast(`Позиция ${id} показана кольцом. Ошибка: ${distance.toFixed(1)}°.`);}renderAtlas();}

function renderPanel(){const focus=currentObject();if(!focus)return;const visible=visibleObjects().map(({object})=>object).sort((a,b)=>a.properties.mag-b.properties.mag);if(mode==='catalog'){missionPanel.innerHTML=`<p class="panel-kicker">КАТАЛОГ · ПАНОРАМА 180°</p><h2>Вращайте<br />небесную сферу</h2><p class="panel-text">Карта 180° с отмеченными объектами Мессье. Зажмите её мышью или пальцем и потяните в любую сторону.</p><div class="field-card"><span class="field-card__label">ТЕКУЩИЙ ОРИЕНТИР</span><strong>${focus.id}</strong><small>${objectName(focus)} · ${objectType(focus)}</small></div><div class="object-list">${visible.slice(0,8).map((object)=>`<div class="object-list__item"><span class="object-list__id">${object.id}</span><span><b class="object-list__name">${objectName(object)}</b><small class="object-list__type">${objectType(object)}</small></span><span class="object-list__mag">${object.properties.mag.toFixed(1)}m</span></div>`).join('')}</div><button class="button panel-button" data-action="new-map" type="button">Вернуть исходный вид <span>↺</span></button><button class="panel-subbutton" data-action="start-learn" type="button">Вернуться к урокам 36°</button>`;return;}if(mode==='learn'){const lesson=field.lesson,objects=lesson.ids.map(objectById).filter(Boolean);if(learnPhase==='study'){missionPanel.innerHTML=`<p class="panel-kicker">ОБУЧЕНИЕ · ПОЛЕ ${lesson.fov}°</p><h2>${lesson.title}</h2><p class="panel-text">Сначала изучите реальные позиции объектов и звёздные ориентиры. Затем начните проверку и расставьте все метки самостоятельно.</p><div class="field-card"><span class="field-card__label">ОБЪЕКТОВ НА КАРТЕ</span><strong>${objects.length}</strong><small>${lesson.description}</small></div><div class="object-list">${objects.map((object)=>`<div class="object-list__item"><span class="object-list__id">${object.id}</span><span><b class="object-list__name">${objectName(object)}</b><small class="object-list__type">${objectType(object)}</small></span><span class="object-list__mag">${object.properties.mag.toFixed(1)}m</span></div>`).join('')}</div><button class="button panel-button" data-action="start-lesson-check" type="button">Начать проверку <span>→</span></button>`;return;}const correct=lesson.ids.filter((id)=>learnResults[id]?.correct).length,passed=learnPhase==='passed',note=passed?`<p class="answer-note">Участок пройден: ${correct} из ${lesson.ids.length} объектов стоят точно на своих местах.</p>`:`<p class="answer-note ${Object.keys(learnResults).length?'is-wrong':''}">${Object.keys(learnResults).length?`Верно: ${correct} из ${lesson.ids.length}. Красные метки передвиньте и проверьте снова.`:'Перетащите все метки из боковой панели на соответствующие места карты.'}</p>`;missionPanel.innerHTML=`<p class="panel-kicker">ПРОВЕРКА · ПОЛЕ ${lesson.fov}°</p><h2>${lesson.title}</h2><p class="panel-text">Объекты скрыты. Участок не будет засчитан, пока все метки не окажутся в правильных координатах.</p>${markerTray(lesson.ids)}${note}${passed?`<button class="button panel-button" data-action="next-lesson" type="button">Следующий участок <span>→</span></button>`:`<button class="button panel-button" data-action="check-lesson" type="button" ${lesson.ids.every((id)=>placedMarkers[id])?'':'disabled'}>Проверить расстановку <span>✓</span></button>`}<button class="panel-subbutton" data-action="restart-lesson" type="button">Вернуться к карте с объектами</button>`;return;}const target=currentTarget||focus,hasMarker=Boolean(placedMarkers[target.id]),result=practiceResult?`<p class="answer-note ${practiceResult.correct?'':'is-wrong'}">${practiceResult.correct?'Точно! Метка и объект совпали в пределах 2,5°.':'Кольцо показывает верное положение. Метку можно снова перетащить и проверить ещё раз.'}</p>`:'<p class="answer-note">Перетащите метку из боковой панели на карту, затем нажмите «Проверить».</p>';missionPanel.innerHTML=`<p class="panel-kicker">ТРЕНИРОВКА · ПАНОРАМА 180°</p><h2>Найдите<br />объект</h2><p class="panel-text">Карта 180° вращается мышью или пальцем. Подписи Мессье скрыты, но настоящие звёзды и линии созвездий остаются.</p><div class="target-card"><span class="target-card__label">ВАША ЦЕЛЬ</span><strong>${target.id}</strong><span>${objectName(target)}<br />${objectType(target)}</span></div>${markerTray([target.id],'ВАША ПЕРЕНОСИМАЯ МЕТКА')}<div class="score-grid"><div><b>${score}</b><span>ТОЧНО НАЙДЕНО</span></div><div><b>${streak}</b><span>ТЕКУЩАЯ СЕРИЯ</span></div></div>${result}<button class="button panel-button" data-action="check-practice" type="button" ${hasMarker?'':'disabled'}>Проверить позицию <span>✓</span></button>${practiceResult?`<button class="panel-subbutton" data-action="next-practice" type="button">Следующий объект</button>`:''}<button class="panel-subbutton" data-action="new-map" type="button">Новая полусфера</button>`;}
function renderAtlasText(){const focus=currentObject();if(!focus)return;const isCatalog=mode==='catalog',isLearn=mode==='learn',isQuiz=isLearn&&learnPhase!=='study',isRotatable=isCatalog||mode==='practice',title=isCatalog?'Каталог объектов Мессье':isLearn?field.lesson.title:`Тренировка: ${focus.id}`,description=isCatalog?'Вращаемая полусфера 180° с отмеченными объектами Мессье. Нажмите на точку, чтобы узнать её.':isLearn?(isQuiz?'Расставьте все переносимые метки на реальных координатах объектов и подтвердите результат.':`Учебное поле ${field.fov}° с несколькими объектами Мессье. Все расстояния и положения звёзд соответствуют координатам J2000.`):`Вращаемая небесная полусфера 180°. Перетащите метку ${focus.id} на правильное место и проверьте её.`;document.querySelector('#atlas-title').textContent=title;document.querySelector('#atlas-kicker').innerHTML=`<span class="eyebrow__dot"></span> ${isCatalog?'Каталог · отмеченные объекты':isLearn?(isQuiz?'Проверка · расстановка меток':`Обучение · поле ${field.fov}°`):'Тренировка · вращаемая полусфера'}`;document.querySelector('#atlas-description').textContent=description;document.querySelector('#map-coordinate').textContent=formatCoordinates(field.center);document.querySelector('#map-fov').textContent=`ПОЛЕ ${field.fov}°`;document.querySelector('#map-hint').textContent=isCatalog?'Потяните карту для вращения или нажмите на объект':isQuiz?'Перетаскивайте метки с панели на карту':mode==='learn'?'Нажмите на красную точку, чтобы узнать объект':'Потяните карту для вращения или перетащите метку';document.querySelector('#objects-control').style.opacity=mode==='practice'||isQuiz?'.42':'1';controls.objects.disabled=mode==='practice'||isQuiz;atlasCanvas.classList.toggle('is-rotatable',isRotatable);mapStage.classList.toggle('is-rotatable',isRotatable);}
function renderAtlas(){if(!sky.ready)return;const isCatalog=mode==='catalog',showLessonObjects=mode==='learn'&&learnPhase==='study';renderAtlasText();renderPanel();drawSky(atlasCanvas,field.center,{grid:isCatalog?false:controls.grid.checked,figures:controls.figures.checked,names:isCatalog?false:controls.names.checked,objects:(isCatalog||showLessonObjects)&&controls.objects.checked,objectLabels:isCatalog||showLessonObjects,fov:field.fov});}
function renderPreview(){if(sky.ready)drawSky(homeCanvas,[-97,31],{grid:false,figures:true,names:false,objects:false,objectLabels:false,fov:180});}
function renderAll(){renderPreview();if(!document.querySelector('#atlas-view').hidden)renderAtlas();}

function start(nextMode){mode=nextMode;suppressClick=false;placedMarkers={};learnResults={};practiceResult=null;if(nextMode==='learn'){chooseLesson();}else if(nextMode==='practice'){score=0;streak=0;choosePracticeField();}else{chooseCatalogField();}setView('atlas');renderAll();}
function newMap(){if(mode==='catalog')chooseCatalogField();else if(mode==='learn')chooseLesson();else choosePracticeField();renderAtlas();showToast(mode==='catalog'?'Каталог возвращён к исходному виду.':mode==='practice'?'Открыта новая полусфера для тренировки.':'Открыта новая учебная область.');}
function nextPractice(){choosePracticeField();renderAtlas();}
function pointFromClient(clientX,clientY){const rect=atlasCanvas.getBoundingClientRect(),radius=Math.min(rect.width,rect.height)*.462,x=clientX-rect.left-rect.width/2,y=clientY-rect.top-rect.height/2;if(Math.hypot(x,y)>radius)return null;return{x,y,radius,coordinates:inverseProject(x,y,field.center,radius,field.fov)};}
function canvasCoordinates(event){return pointFromClient(event.clientX,event.clientY);}
function hitPlacedMarker(point){if(!point)return null;let match=null,min=18;placementIds().forEach((id)=>{if(!placedMarkers[id])return;const markerPoint=project(placedMarkers[id],field.center,point.radius,0,0,field.fov);if(!markerPoint)return;const distance=Math.hypot(markerPoint.x-point.x,markerPoint.y-point.y);if(distance<min){min=distance;match=id;}});return match;}
function mapClick(event){if(!sky.ready||suppressClick){suppressClick=false;return;}const click=canvasCoordinates(event);if(!click)return;if(mode==='catalog'||(mode==='learn'&&learnPhase==='study')){const hit=displayedObjects().map(({object})=>({object,point:project(object.geometry.coordinates,field.center,click.radius,0,0,field.fov)})).find(({point})=>Math.hypot(point.x-click.x,point.y-click.y)<14);if(hit)showToast(`${hit.object.id} — ${objectName(hit.object)}. ${objectType(hit.object)}, ${hit.object.properties.mag.toFixed(1)}m.`);}}
function beginRotation(event,point){dragState={type:'rotation',pointerId:event.pointerId,capture:atlasCanvas,startX:point.x,startY:point.y,startCenter:[...field.center],radius:point.radius,moved:false};atlasCanvas.setPointerCapture?.(event.pointerId);}
function createMarkerGhost(id){const ghost=document.createElement('div');ghost.textContent=id;ghost.style.cssText='position:fixed;z-index:99;pointer-events:none;padding:8px 11px;border:2px solid #1d68e8;border-radius:8px;background:#fff;color:#1d68e8;font:800 12px Inter,Arial,sans-serif;box-shadow:0 8px 24px #1d68e833;transform:translate(-50%,-50%)';document.body.append(ghost);return ghost;}
function beginTokenDrag(event,id,source){event.preventDefault();const ghost=createMarkerGhost(id);ghost.style.left=`${event.clientX}px`;ghost.style.top=`${event.clientY}px`;dragState={type:'marker',pointerId:event.pointerId,id,capture:source,ghost,startX:event.clientX,startY:event.clientY,moved:false,fromCanvas:false};source.setPointerCapture?.(event.pointerId);}
function beginCanvasMarkerDrag(event,id,point){event.preventDefault();dragState={type:'marker',pointerId:event.pointerId,id,capture:atlasCanvas,startX:event.clientX,startY:event.clientY,moved:false,fromCanvas:true,lastPoint:point};atlasCanvas.setPointerCapture?.(event.pointerId);}
function startCanvasPointer(event){if(!sky.ready)return;const point=canvasCoordinates(event);if(!point)return;const markerId=hitPlacedMarker(point);if(markerId){beginCanvasMarkerDrag(event,markerId,point);return;}if(['catalog','practice'].includes(mode))beginRotation(event,point);}
function movePointer(event){if(!dragState||event.pointerId!==dragState.pointerId)return;if(dragState.type==='rotation'){const point=canvasCoordinates(event);if(!point)return;const dx=point.x-dragState.startX,dy=point.y-dragState.startY,degreePerPixel=field.fov/(2*dragState.radius);if(Math.hypot(dx,dy)>2)dragState.moved=true;field.center=[wrapLon(dragState.startCenter[0]+dx*degreePerPixel),clamp(dragState.startCenter[1]+dy*degreePerPixel,-84,84)];renderAtlas();return;}if(Math.hypot(event.clientX-dragState.startX,event.clientY-dragState.startY)>2)dragState.moved=true;if(dragState.ghost){dragState.ghost.style.left=`${event.clientX}px`;dragState.ghost.style.top=`${event.clientY}px`;}const point=pointFromClient(event.clientX,event.clientY);if(dragState.fromCanvas&&point?.coordinates){setPlacedMarker(dragState.id,point.coordinates);renderAtlas();}}
function endPointer(event){if(!dragState||event.pointerId!==dragState.pointerId)return;const state=dragState,point=pointFromClient(event.clientX,event.clientY);state.capture.releasePointerCapture?.(event.pointerId);if(state.type==='marker'){if(point?.coordinates){setPlacedMarker(state.id,point.coordinates);renderAtlas();}state.ghost?.remove();}suppressClick=state.moved;dragState=null;setTimeout(()=>{suppressClick=false;},0);}

document.addEventListener('click',(event)=>{const action=event.target.closest('[data-action]')?.dataset.action;if(!action)return;if(action==='home')setView('home');if(action==='open-tour')setView('tour');if(action==='start-learn')start('learn');if(action==='start-practice')start('practice');if(action==='start-catalog')start('catalog');if(action==='new-map')newMap();if(action==='next-lesson'){chooseLesson();renderAtlas();}if(action==='start-lesson-check')startLessonCheck();if(action==='check-lesson')checkLesson();if(action==='restart-lesson'){learnPhase='study';placedMarkers={};learnResults={};renderAtlas();}if(action==='check-practice')checkPractice();if(action==='next-practice')nextPractice();});
document.addEventListener('pointerdown',(event)=>{const token=event.target.closest('[data-marker-token]');if(token)beginTokenDrag(event,token.dataset.markerId,token);});
atlasCanvas.addEventListener('click',mapClick);atlasCanvas.addEventListener('pointerdown',startCanvasPointer);document.addEventListener('pointermove',movePointer);document.addEventListener('pointerup',endPointer);document.addEventListener('pointercancel',endPointer);Object.values(controls).forEach((control)=>control.addEventListener('change',renderAtlas));window.addEventListener('resize',()=>requestAnimationFrame(renderAll));
async function loadCatalog(){try{const responses=await Promise.all(Object.values(urls).map((url)=>fetch(url)));if(responses.some((response)=>!response.ok))throw new Error('Каталог недоступен');const[stars,lines,messier,names]=await Promise.all(responses.map((response)=>response.json()));sky.stars=stars.features;sky.lines=lines.features;sky.messier=messier.features;sky.names=names;sky.ready=true;mapLoading.classList.add('is-hidden');document.querySelector('#status-dot').classList.add('is-ready');document.querySelector('#status-text').textContent='КАТАЛОГ ЗАГРУЖЕН · J2000';chooseLesson();renderAll();}catch(error){document.querySelector('#map-loading').innerHTML='<p>Не удалось загрузить каталог.<br />Проверьте подключение к интернету и обновите страницу.</p>';document.querySelector('#status-text').textContent='КАТАЛОГ НЕДОСТУПЕН';showToast('Каталог не загрузился: нужен доступ к открытому астрономическому источнику.');}}
loadCatalog();
